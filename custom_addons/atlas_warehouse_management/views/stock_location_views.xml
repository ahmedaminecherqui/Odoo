
<odoo>
    <record id="view_location_form_abc" model="ir.ui.view">
        <field name="name">stock.location.form.abc</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='usage']" position="after">
                <field name="abc_category" widget="priority" invisible="usage != 'internal'"/>
            </xpath>
        </field>
    </record>

    <record id="view_location_tree_abc" model="ir.ui.view">
        <field name="name">stock.location.tree.abc</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='usage']" position="after">
                <field name="abc_category" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
